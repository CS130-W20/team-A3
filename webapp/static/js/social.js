function handeling_friend_request() {
    alert("we know that a friend request is sent to user " + show_user.id + " " + show_user.name + "\n"
        + "but it is not handled yet");
}

function user_chat() {
    alert("we know that you want to chat with user " + show_user.id + " " + show_user.name + "\n"
        + "but it is not implemented yet");
}